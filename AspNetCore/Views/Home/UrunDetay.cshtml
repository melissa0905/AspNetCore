@model Urun
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <img src="" class="w-100 img-thumbnail img-fluid" alt="" />
            </div>
            <div class="col-md-9">
                <div class="card">
                    <h5 class="card-header">@Model.Ad</h5>
                    <div class="card-body">
                        <h5 class="card-title">@Model.Ad</h5>
                        <p class="card-text">
                            <strong>Kategorileri</strong><getirKategoriAd urun-id="@Model.Id">
                            </getirKategoriAd>
                        @Model.Fiyat</p>
                        <a href="#" class="btn btn-primary ml-1 float-right">Sepete Ekle</a>
                        <a asp-action="Index" asp-controller="Home" class="btn btn-danger float-right">Geri Dön</a>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>